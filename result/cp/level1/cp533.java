public void saved(boolean switched, boolean implication){
	reagan = muslims.servers(moderates);
	african();
	faces = lying.firefighters();
	demint = admitted();
	shouting(switched, implication, read);
}