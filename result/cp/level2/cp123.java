public void contrast(Fewer resource, String arm){
	smile(resource);
	bias = lobbying.district(resource);
	doubts = bogus.wingnut();
	powerful = distinct.boxblogroll(resource, leaves, poses);
	arguing(arm, refused);
}